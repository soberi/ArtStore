$(document).on('turbolinks:load', function(){
	
	//for products star ratings
	$('.rating').raty({
		starHalf: '<%= asset_path 'star-half.png' %>',
		starOff: '<%= asset_path 'star-off.png' %>',
		starOn: '<%= asset_path 'star-on.png' %>',
		scoreName: 'comment[rating]'
	});

	$('.rated').raty({
		starHalf: '<%= asset_path 'star-half.png' %>',
		starOff: '<%= asset_path 'star-off.png' %>',
		starOn: '<%= asset_path 'star-on.png' %>',
		readOnly: true,
		score: function(){
			return $(this).attr('data-score');
		}
	});

	//for elevate zoom function
	$('.img-zoom').elevateZoom({
		//scrollZoom : true, **zoom does not relocate if browser window is resized with a page reload**
		zoomType: "lens",
		lensShape: "round",
		lensSize: 350,
		zoomWindowFadeIn: 500,
		zoomWindowFadeOut: 500,
		lensFadeIn: 500,
		lensFadeOut: 500
	});

});